{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm build --filter=@saas/closer-web",
  "outputDirectory": ".next",
  "installCommand": "echo 'install handled by buildCommand'"
}
